<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Verify OTP</title>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            height: 100vh;
            background: linear-gradient(135deg, #4e73df, #3b5bdb);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .otp-card {
            max-width: 400px;
            width: 100%;
            border-radius: 16px;
            padding: 32px;
            background: #ffffff;
        }

        .otp-icon {
            width: 72px;
            height: 72px;
            background: #4e73df;
            color: #fff;
            font-size: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 18px;
        }

        .otp-input {
            letter-spacing: 12px;
            font-size: 22px;
            font-weight: 600;
            text-align: center;
            border-radius: 12px;
            height: 52px;
        }

        .btn-verify {
            background: #4e73df;
            border: none;
            border-radius: 12px;
            font-weight: 500;
            padding: 10px;
        }

        .btn-verify:hover {
            background: #3b5bdb;
        }

        .text-muted {
            font-size: 14px;
        }
    </style>
</head>

<body>

    <div class="card otp-card shadow-lg border-0 text-center">
        <div class="otp-icon">🔐</div>

        <h4 class="mb-2 font-weight-bold">OTP Verification</h4>
        <p class="text-muted mb-4">
            Enter the 6-digit OTP sent to your registered email
        </p>

        <form action="/otpVerify" method="POST">
            <div class="form-group">
                <input type="text"
                       name="adminOTP"
                       class="form-control otp-input"
                       maxlength="6"
                       placeholder="______"
                       required>
            </div>

            <button type="submit" class="btn btn-primary btn-block btn-verify">
                Verify OTP
            </button>
        </form>
    </div>

</body>
</html>
